import * as BasicElement from "@features/recovery-zone/types/BasicElement";

const BottomSheet = () => {
  return (
    <BasicElement.Wrapper $ColumnDirection={false} $padding={20} $gap={10}>
      {/* Content goes here */}
    </BasicElement.Wrapper>
  );
};

export default BottomSheet;